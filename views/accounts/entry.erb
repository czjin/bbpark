<!DOCTYPE html>
<%#include "accounts.h" %>
<% tfetch(QVariantMap, accounts); %>
<html>
<head>
  <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
  <title><%= controller()->name() + ": " + controller()->activeAction() %></title>
</head>
<body>
<p style="color: red"><%=$ error %></p>
<p style="color: green"><%=$ notice %></p>

<h1>New Accounts</h1>

<%== formTag(urla("create"), Tf::Post) %>
  <p>
    <label>Acc No<br /><input name="accounts[accNo]" value="<%= accounts["accNo"] %>" /></label>
  </p>
  <p>
    <label>User ID<br /><input name="accounts[userId]" value="<%= accounts["userId"] %>" /></label>
  </p>
  <p>
    <label>Pay Code<br /><input name="accounts[payCode]" value="<%= accounts["payCode"] %>" /></label>
  </p>
  <p>
    <label>Balance<br /><input name="accounts[balance]" value="<%= accounts["balance"] %>" /></label>
  </p>
  <p>
    <label>Credit<br /><input name="accounts[credit]" value="<%= accounts["credit"] %>" /></label>
  </p>
  <p>
    <label>Credit Available<br /><input name="accounts[creditAvailable]" value="<%= accounts["creditAvailable"] %>" /></label>
  </p>
  <p>
    <label>Status<br /><input name="accounts[status]" value="<%= accounts["status"] %>" /></label>
  </p>
  <p>
    <label>Type<br /><input name="accounts[type]" value="<%= accounts["type"] %>" /></label>
  </p>
  <p>
    <label>Issuer<br /><input name="accounts[issuer]" value="<%= accounts["issuer"] %>" /></label>
  </p>
  <p>
    <label>Memo<br /><input name="accounts[memo]" value="<%= accounts["memo"] %>" /></label>
  </p>
  <p>
    <input type="submit" value="Create" />
  </p>
</form>

<%== linkTo("Back", urla("index")) %>

</body>
</html>
